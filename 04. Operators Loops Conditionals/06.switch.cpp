#include <iostream>
using namespace std;

int main() {

    int a;
    cout << "Enter the value for a : ";
    cin >> a;
    cout << endl;

    // chars, string, expression, 2 same cases, neg value, float, sentence, bool, 
    // continue intead of break, operator

    switch (a) {
        case 1: 
            break;
        case 2: 
            break;
        case 3: 
            break;
        default:
            cout << "default";
    }

    cout << endl;
    cout << endl;
    cout << endl;
    return 0;
}
