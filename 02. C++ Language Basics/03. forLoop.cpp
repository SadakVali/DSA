#include <iostream>
using namespace std;

int main()
{
  // // prints the numnbers [0, 5)
  // for (int iter = 0; iter < 5; iter = iter + 1)
  // {
  //   cout << iter << endl;
  // }

  // // prints the numnbers [5, 1]
  // for (int iter = 5; iter > 0; iter = iter - 1)
  // {
  //   cout << iter << endl;
  // }

  // // 2th multiples from 1 to 10
  // for (int iter = 1; iter <= 10; iter = iter + 1)
  // {
  //   cout << 2 * iter << endl;
  // }

  // // prints even numbers in range [0, 5] and 0 itself
  // for (int iter = 0; iter <= 5; iter = iter + 2) {
  //   cout << iter << endl;
  // }

  // // prints the odd numbers in range [1, 5]
  // for (int iter = 1; iter <= 5; iter = iter + 2)
  // {
  //   cout << iter << endl;
  // }

  // // multiplicative increments in for loop
  // for (int iter = 1; iter < 10; iter = iter * 2)
  // {
  //   cout << iter << endl;
  // }

  // // divisive decrements
  // for (int iter = 100; iter > 0; iter = iter / 2)
  // {
  //   cout << iter << endl;
  // }

  // // complex conditioning in the for loop
  // for (int iter = 5; (iter >= 0 && iter <= 10); iter = iter + 1)
  // {
  //   cout << iter << endl;
  // }

  // // mandatory inputs to the for loop
  // int iter = 0;
  // for (;;)
  // {
  //   if (iter < 5)
  //   {
  //     cout << iter << endl;
  //     iter = iter + 1;
  //   }
  //   else
  //   {
  //     break;
  //   }
  // }

  cout << endl;
}